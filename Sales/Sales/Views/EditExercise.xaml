<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Sales.Views.EditExercise">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Padding="10" Spacing="10">
                <Label Text="Nombre del Ejercicio " FontAttributes="Bold"></Label>
                <Entry x:Name="nombreejercicioEntry"
                        Placeholder="Nombre" />
                <Label Text="DESCRIPCIÓN " FontAttributes="Bold"></Label>
                <Entry x:Name="descripcionEntry"
                        Placeholder="descripcion" />

                <StackLayout Orientation="Horizontal">
                    <Button x:Name="actualizarButton"
                            Text="Actualizar"
                            BackgroundColor="Teal"
                            TextColor="White"
                            HorizontalOptions="FillAndExpand"
                            VerticalOptions="Center" />
                    <Button x:Name="borrarButton"
                            Text="Borrar"
                            BackgroundColor="Red"
                            TextColor="White"
                            HorizontalOptions="FillAndExpand"
                            VerticalOptions="Center" />

                </StackLayout>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>